#include <stdio.h>

/**
 * main - the principal
 * Return: 0 Always seccsees
 * @argc: the first input
 * @argv: the second input
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", *argv);
	return (0);
}
