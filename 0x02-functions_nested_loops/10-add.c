#include <stdio.h>

/**
* add - adds two integers and returns the result.
* Return: return the result.
* @a: the first input of the function add.
* @b: the second input of the function add.
*/


int add(int a, int b)
{
	int ad;

	ad = a + b;
	return (ad);
}
